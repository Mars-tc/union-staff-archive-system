# 工会职工档案系统使用文档

本文档为工会职工档案系统的完整使用指南，涵盖系统安装配置、各功能模块操作说明、业务流程详解及常见问题排查。

---

## 目录

1. [系统概述](#系统概述)
2. [快速上手](#快速上手)
3. [用户角色与权限](#用户角色与权限)
4. [登录与账户管理](#登录与账户管理)
5. [首页仪表盘](#首页仪表盘)
6. [工会会员管理](#工会会员管理)
7. [会费授权管理](#会费授权管理)
8. [困难帮扶](#困难帮扶)
9. [爱心互助会](#爱心互助会)
10. [爱心帮扶](#爱心帮扶)
11. [审核任务](#审核任务)
12. [统计报表](#统计报表)
13. [系统管理](#系统管理)
14. [数据库结构](#数据库结构)
15. [部署指南](#部署指南)
16. [常见问题排查](#常见问题排查)

---

## 系统概述

工会职工档案系统是面向工会组织的一站式办公平台，实现了以下核心业务的全流程数字化管理：

- **工会会员管理**：电子化入会申请、两级审核、会员档案
- **会费授权管理**：在线授权会费扣划、审核流程
- **困难帮扶**：困难职工申请、审批、档案管理、批量导入
- **爱心互助会**：入会申请、审核管理
- **爱心帮扶**：爱心互助会会员的帮扶申请与审批
- **统计分析**：各类业务数据统计与报表

### 技术架构

```
┌─────────────────────────────────────────────┐
│                 前端 (React)                │
│  ┌─────────┐ ┌─────────┐ ┌──────────────┐  │
│  │ 页面组件 │ │ 通用组件 │ │ 状态管理     │  │
│  └─────────┘ └─────────┘ └──────────────┘  │
└──────────────────┬──────────────────────────┘
                   │ HTTP/JSON (Vite Proxy)
┌──────────────────┴──────────────────────────┐
│                后端 (Express)               │
│  ┌─────────┐ ┌─────────┐ ┌──────────────┐  │
│  │ 路由层   │ │ 中间件   │ │ 数据访问层   │  │
│  └─────────┘ └─────────┘ └──────────────┘  │
└──────────────────┬──────────────────────────┘
                   │ pg 驱动
┌──────────────────┴──────────────────────────┐
│              数据库 (PostgreSQL)            │
└─────────────────────────────────────────────┘
```

---

## 快速上手

### 前置条件

| 软件 | 版本要求 | 说明 |
|------|----------|------|
| Node.js | >= 18 | JavaScript 运行时 |
| PostgreSQL | >= 14 | 关系型数据库 |
| pnpm | >= 9 | 包管理器 |

### 30 分钟快速搭建

#### 第一步：安装 PostgreSQL

1. 下载并安装 PostgreSQL（[官方下载页](https://www.postgresql.org/download/)）
2. 安装时设置超级用户密码
3. 确保 PostgreSQL 服务已启动

#### 第二步：安装 Node.js 依赖

```bash
# 安装 pnpm（如果尚未安装）
npm install -g pnpm

# 进入项目目录并安装依赖
cd union-staff-archive-system
pnpm install
```

#### 第三步：配置数据库

```powershell
# 创建数据库（Windows PowerShell）
Set-Item -Path env:PGCLIENTENCODING -Value UTF8
psql -U postgres -c "CREATE DATABASE union_staff ENCODING 'UTF8';"
```

#### 第四步：初始化数据库

```powershell
# 执行初始化脚本（创建表和初始数据）
Set-Item -Path env:PGCLIENTENCODING -Value UTF8
psql -U postgres -d union_staff -f api/database/init.sql
```

#### 第五步：配置环境变量

项目根目录下的 `.env` 文件：

```env
PORT=3001
DB_HOST=localhost
DB_PORT=5432
DB_NAME=union_staff
DB_USER=postgres
DB_PASSWORD=your_password_here
JWT_SECRET=union_staff_jwt_secret_key_2024
```

> ⚠️ 生产环境请务必修改默认密钥和数据库密码。

#### 第六步：启动系统

```bash
pnpm run dev
```

启动成功后：
- 前端界面：访问 http://localhost:5173
- 后端 API：访问 http://localhost:3001/api

#### 第七步：登录系统

使用默认管理员账户登录：
- 邮箱：`admin@union.com`
- 密码：`password`

---

## 用户角色与权限

### 角色体系

系统采用基于角色的访问控制（RBAC），分为四种角色：

```
管理员 (admin)
├── 用户管理（创建/编辑/删除用户）
├── 模块权限分配
├── 所有业务审核（入会/会费/帮扶）
├── 批量导入困难档案
├── 系统日志查看
├── 统计报表
└── 任务统计

基层审核人 (grass_root_auditor)
├── 第一级审核权（入会/会费/帮扶）
└── 待办/已办任务查看

委员会审核人 (union_committee_auditor)
├── 第二级审核权（入会/会费/帮扶）
└── 待办/已办任务查看

普通职工 (employee)
├── 个人资料管理
├── 提交入会申请
├── 提交会费授权
├── 提交困难帮扶（需工会会员身份）
├── 提交爱心互助会申请
├── 提交爱心帮扶（需爱心互助会会员身份）
└── 查看困难档案
```

### 会员类型

用户可同时拥有两种会员身份：

| 会员类型 | 字段 | 获取方式 | 权益 |
|----------|------|----------|------|
| 工会会员 | union_member | 入会申请审批通过后自动获取 | 申请困难帮扶、会费授权 |
| 爱心互助会会员 | mutual_aid_member | 爱心互助会申请审批通过后自动获取 | 申请爱心帮扶 |

### 模块权限

管理员可为每个用户分配以下模块权限：

| 模块代码 | 模块名称 | 适用角色 |
|----------|----------|----------|
| tasks | 任务管理 | 审核人、管理员 |
| membership_audit | 入会审核 | 审核人、管理员 |
| fee_audit | 会费审核 | 管理员 |
| mutual_aid_audit | 互助会审核 | 管理员 |
| mutual_aid_difficulty_audit | 爱心帮扶审批 | 审核人、管理员 |
| user_management | 用户管理 | 管理员 |
| system_logs | 系统日志 | 管理员 |
| statistics | 查询统计 | 管理员 |

> 💡 模块权限与角色独立配置。即使角色为审核人，也需要分配相应模块才能在导航中看到对应菜单。

---

## 登录与账户管理

### 登录系统

1. 打开登录页面
2. 支持三种登录方式：
   - 邮箱地址（如 `user@union.com`）
   - 手机号（如 `13800138000`）
   - 身份证号
3. 输入密码
4. 点击"登录"

### 注册新账户

1. 点击登录页面的"注册"链接
2. 填写邮箱、密码、姓名、手机号
3. 点击"注册"完成注册

### 忘记密码

1. 点击登录页面的"忘记密码"链接
2. 输入注册邮箱
3. 系统发送验证码（开发模式下会直接显示）
4. 输入验证码和新密码
5. 点击"重置密码"

### 修改密码

1. 登录后进入"个人资料"页面
2. 点击"修改密码"
3. 输入当前密码和新密码
4. 点击"确认"提交

### 更新个人资料

1. 进入"个人资料"页面
2. 可修改以下信息：
   - 姓名、手机号、邮箱
   - 性别、民族、政治面貌
   - 头像照片（Base64 格式）
   - 籍贯、身份证号
   - 户口地址、居住地址
   - 工作单位、部门、职位
   - 学历、工作经历、家庭成员、专长
3. 点击"保存"提交

---

## 首页仪表盘

登录成功后进入首页仪表盘，展示以下内容：

### 统计卡片

| 卡片 | 说明 |
|------|------|
| 待审批数量 | 当前用户待处理的审批总数 |
| 已完成申请 | 已审批的申请总数 |
| 工会会员数 | 系统中工会会员总数 |
| 本月帮扶金额 | 本月已审批通过的帮扶金额汇总 |

### 快捷入口

- 快速进入入会申请
- 快速进入困难帮扶申请
- 快速进入爱心互助会申请
- 快速进入待办任务

### 最近动态

显示用户最近的操作记录和系统动态。

---

## 工会会员管理

### 提交入会申请

**适用角色**：普通职工（非工会会员）

**操作步骤**：

1. 进入"入会申请"页面
2. 填写申请信息：
   - **必填项**：部门、职位、电子签名
   - **选填项**：性别、民族、政治面貌、学历、身份证号、籍贯、户口地址、居住地址、联系电话、工作经历、家庭成员、专长
3. 在电子签名板上签署姓名
4. 点击"提交申请"

**业务规则**：
- 已经是工会会员的用户无法重复申请
- 已有审批中的申请无法再次提交
- 申请提交后进入"待基层审核"状态

### 审核入会申请

**适用角色**：基层审核人、委员会审核人、管理员

#### 基层审核

1. 进入"入会审核"页面
2. 查看待审核列表
3. 点击申请详情
4. 填写基层审核意见
5. 在电子签名板上签名
6. 点击"审核通过"或"拒绝"

#### 委员会审核

1. 基层审核通过后，申请自动流转到委员会审核
2. 委员会审核人登录后可在待办中看到
3. 填写委员会审核意见并签名
4. 点击"审核通过"或"拒绝"

#### 批量审核

1. 在审核列表中勾选多个申请
2. 点击"批量审核"
3. 填写审核意见（批量共用）
4. 确认批量通过或拒绝

#### 标记已核

审核完成后可标记为"已核"，方便区分已处理的申请与仍需关注的申请。

### 申请标签

可为入会申请添加标签，便于分类管理和检索。

---

## 会费授权管理

### 提交会费授权

**适用角色**：工会会员

**操作步骤**：

1. 进入"会费授权"页面
2. 填写授权信息：
   - **必填项**：生效日期、电子签名
3. 在电子签名板上签名
4. 点击"提交授权"

**业务规则**：
- 仅工会会员可提交
- 已有审批中的申请无法重复提交
- 授权通过后会费将按授权日期开始从工资中扣划

### 审核会费授权

**适用角色**：管理员

审核流程与入会申请相同，采用两级审核机制。

---

## 困难帮扶

### 申请困难帮扶

**适用角色**：工会会员

**操作步骤**：

1. 进入"困难帮扶申请"页面
2. 选择困难类别：
   - 伤残致困
   - 意外致困
   - 因病致困（需选择病种）
   - 子女助学
   - 特殊困难
3. 如选择"因病致困"，需选择具体病种：
   - 重疾类：恶性肿瘤、严重心血管疾病、慢性肾功能衰竭、严重肝病
   - 慢病类：糖尿病、高血压、心脏病
   - 其他类：其他疾病
4. 填写申请信息：
   - **必填项**：帮扶金额、申请原因、电子签名
   - **选填项**：家庭年收入、家庭成员、银行账号、开户行、账户名
5. 如同时是爱心互助会会员，可勾选"同步申请爱心帮扶"
6. 在电子签名板上签名
7. 点击"提交申请"

**业务规则**：
- 仅工会会员可申请
- 同一病种仅能申请一次（重复检查，前后端双重校验）
- 若勾选"同步申请爱心帮扶"，系统将同时创建爱心帮扶申请

### 上传证明材料

提交申请后可上传诊断证明、困难证明等材料：
1. 在申请详情页面点击"上传材料"
2. 选择文件（支持常见图片和文档格式）
3. 文件将保存至服务器 `api/uploads/` 目录

### 审核困难帮扶

**适用角色**：基层审核人、委员会审核人、管理员

#### 审核流程

1. 基层审核：基层审核人填写审核意见、签名、设定实际帮扶金额
2. 委员会审核：委员会审核人复核并审批
3. 通过后生成困难职工档案

#### 审批注意事项

- 审批时系统会再次检查病种重复，若该用户已有相同病种的申请（任何状态），将拒绝审批
- 实际帮扶金额可与申请金额不同
- 可标记"一次性领取"或设定帮扶次数

### 查看困难档案

**适用角色**：所有登录用户

困难档案仅展示审批通过的申请，可查看：
- 用户基本信息
- 帮扶记录（病种、金额、时间）
- 家庭情况
- 银行账户信息

### 批量导入困难档案

**适用角色**：管理员

**操作步骤**：

1. 进入困难档案页面，点击"批量导入"
2. 上传 Excel 文件
3. 点击"预览"查看匹配结果
4. 确认匹配情况：
   - 已匹配：系统中已存在的用户
   - 未匹配：系统中不存在的用户
5. 对于未匹配用户，可选择"自动创建"
   - 新用户角色：普通职工（employee）
   - 自动标记为工会会员
   - 系统将生成随机密码并导出账号 Excel
6. 确认后执行导入

**Excel 文件格式要求**：

| 列名 | 必填 | 说明 |
|------|------|------|
| 姓名 | 是 | 职工姓名 |
| 员工编码/身份证号 | 是 | 用于匹配用户 |
| 所属单位 | 是 | 部门信息 |
| 联系电话 | 是 | 联系方式 |
| 家庭年收入（万元） | 否 | 家庭收入 |
| 个人年收入（万元） | 否 | 个人收入 |
| 需要抚养人数 | 否 | 家属人数 |
| 是否退休 | 否 | 是/否 |
| 是否申请过困难帮扶 | 否 | 是/否（影响审批状态） |
| 困难帮扶申请类别 | 否 | 伤残致困/意外致困/因病致困/子女助学/特殊困难 |
| 申请原因 | 否 | 详细原因 |
| 是否属于一次性领取 | 否 | 是/否 |
| 困难帮扶金额（元） | 否 | 帮扶金额 |
| 帮扶次数 | 否 | 历史帮扶次数 |
| 备注 | 否 | 其他说明 |

---

## 爱心互助会

### 提交爱心互助会申请

**适用角色**：所有登录用户

**操作步骤**：

1. 进入"爱心互助会申请"页面
2. 填写申请信息：
   - **必填项**：性别、身份证号、部门、岗位、电子签名
   - **选填项**：出生日期、政治面貌、手机号、家庭电话、家庭地址、邮编、家庭成员
3. 系统自动校验身份证号格式和手机号格式
4. 在电子签名板上签名
5. 点击"提交申请"

**业务规则**：
- 已是爱心互助会会员无法重复申请
- 已有审批中的申请无法重复提交
- 申请通过后自动成为爱心互助会会员

### 审核爱心互助会申请

**适用角色**：管理员

审核流程：
1. 管理员填写工作小组意见
2. 管理员填写办公室意见
3. 审核通过后用户自动获得 `mutual_aid_member` 身份

---

## 爱心帮扶

### 申请爱心帮扶

**适用角色**：爱心互助会会员

**操作步骤**：

1. 进入"爱心帮扶申请"页面
2. 申请方式有两种：
   - **新建申请**：填写与困难帮扶相同的信息
   - **从困难帮扶导入**：点击"导入"按钮，选择历史困难帮扶记录一键导入
3. 导入后可修改信息，也可直接提交
4. 点击"提交申请"

**业务规则**：
- 仅爱心互助会会员可申请
- 同一病种仅能申请一次
- 即使同时是工会会员和爱心互助会会员，两种帮扶申请独立计算

### 审核爱心帮扶

**适用角色**：基层审核人、委员会审核人、管理员

审核流程与困难帮扶相同，采用两级审核机制。

---

## 审核任务

### 待办任务

**适用角色**：基层审核人、委员会审核人、管理员

展示当前用户待处理的审核任务，包括：
- 入会申请待审核
- 会费授权待审核
- 困难帮扶待审核
- 爱心互助会申请待审核
- 爱心帮扶待审核

可点击任务快速跳转到对应审核页面。

### 已办任务

展示当前用户已完成的审核记录，可按类型筛选。

### 任务统计

展示各类型待办/已办任务的数量统计。

---

## 统计报表

**适用角色**：管理员

### 困难帮扶统计

- 待审批/已通过/已拒绝数量
- 帮扶总金额
- 按困难类别分布统计

### 爱心帮扶统计

- 待审批/已通过/已拒绝数量
- 帮扶总金额
- 按困难类别分布统计

### 其他统计

- 会员总数
- 月度帮扶趋势

---

## 系统管理

### 用户管理

**适用角色**：管理员

#### 创建用户

1. 进入"用户管理"页面
2. 点击"新建用户"
3. 填写邮箱、密码、姓名、手机号、角色
4. 点击"创建"

#### 批量创建用户

1. 点击"批量创建"
2. 批量填写用户信息
3. 系统为每个用户生成随机密码
4. 创建完成后显示生成的密码列表

#### 编辑用户

修改用户的姓名、手机号、角色、退休状态。

#### 批量更新

勾选多个用户批量修改角色、会员类型、退休状态。

#### 删除用户

- 不能删除管理员账户
- 批量删除时同样禁止删除管理员

#### 用户筛选

支持按以下条件筛选用户：
- 姓名/邮箱/手机号搜索
- 角色筛选
- 工会会员筛选
- 爱心互助会会员筛选
- 退休状态筛选

### 模块权限管理

**适用角色**：管理员

为用户分配系统模块访问权限：
1. 在用户管理中编辑用户
2. 勾选该用户可访问的模块
3. 保存后生效

### 系统日志

**适用角色**：管理员

查看系统操作日志，包括：
- 登录/登出记录
- 用户创建/修改/删除
- 权限变更
- 申请提交/审核
- 数据导入

支持按操作类型、操作人、时间范围筛选。

---

## 数据库结构

### 表关系图

```
users (用户表)
├── union_members (工会会员表)
├── user_profiles (用户档案表)
├── membership_applications (入会申请表)
├── fee_authorizations (会费授权表)
├── difficulty_applications (困难帮扶申请表)
├── mutual_aid_applications (爱心互助会申请表)
├── mutual_aid_difficulty_applications (爱心帮扶申请表)
└── password_resets (密码重置表)
    │
    └── modules / user_modules (模块权限)

disease_types (病种表)
└── difficulty_applications.disease_type_id
└── mutual_aid_difficulty_applications.disease_type_id

system_logs (系统日志表)
modules (模块表)
user_modules (用户模块关联表)
```

### 核心数据表说明

#### users - 用户表

| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGSERIAL | 主键 |
| email | VARCHAR(255) | 邮箱（唯一） |
| password | VARCHAR(255) | 密码（bcrypt 加密） |
| name | VARCHAR(100) | 姓名 |
| phone | VARCHAR(20) | 手机号 |
| role | VARCHAR(30) | 角色（employee/admin/grass_root_auditor/union_committee_auditor） |
| union_member | BOOLEAN | 是否工会会员 |
| mutual_aid_member | BOOLEAN | 是否爱心互助会会员 |
| department | VARCHAR(100) | 部门 |
| is_retired | BOOLEAN | 是否退休 |
| created_at | TIMESTAMP | 创建时间 |

#### membership_applications - 入会申请表

| 字段 | 说明 |
|------|------|
| user_id | 申请人ID |
| position | 职位 |
| gender | 性别 |
| id_card | 身份证号 |
| signature | 电子签名 |
| status | 状态（pending/approved/rejected） |
| audit_step | 审核步骤（pending/grass_root/union_committee/completed） |
| grass_root_opinion | 基层审核意见 |
| union_committee_opinion | 委员会审核意见 |
| grass_root_signature | 基层审核人签名 |
| union_committee_signature | 委员会审核人签名 |

#### difficulty_applications - 困难帮扶申请表

| 字段 | 说明 |
|------|------|
| user_id | 申请人ID |
| disease_type_id | 病种ID（因病致困时必填） |
| difficulty_category | 困难类别 |
| amount | 申请金额 |
| actual_amount | 实际审批金额 |
| reason | 申请原因 |
| document_path | 证明材料路径 |
| 审核相关字段 | 同入会申请 |

#### mutual_aid_difficulty_applications - 爱心帮扶申请表

结构与 `difficulty_applications` 相同，额外包含 `marked_as_audited` 字段。

#### system_logs - 系统日志表

| 字段 | 说明 |
|------|------|
| user_id | 操作人ID |
| action | 操作类型（login/create_user/update_user 等） |
| resource | 资源类型 |
| resource_id | 资源ID |
| details | 操作详情 |
| ip_address | IP地址 |
| user_agent | 浏览器信息 |

### 病种数据

系统预置 8 个病种：

| 病种 | 类别 |
|------|------|
| 恶性肿瘤 | 重疾 |
| 严重心血管疾病 | 重疾 |
| 慢性肾功能衰竭 | 重疾 |
| 严重肝病 | 重疾 |
| 糖尿病 | 慢病 |
| 高血压 | 慢病 |
| 心脏病 | 慢病 |
| 其他疾病 | 其他 |

---

## 部署指南

### 生产环境部署

#### 1. 构建前端

```bash
pnpm run build
```

构建产物输出到 `dist/` 目录。

#### 2. 环境变量配置

生产环境 `.env` 示例：

```env
PORT=3001
DB_HOST=your_db_host
DB_PORT=5432
DB_NAME=union_staff
DB_USER=your_db_user
DB_PASSWORD=your_secure_password
JWT_SECRET=your_production_jwt_secret_key
NODE_ENV=production
```

#### 3. 启动后端

```bash
# 使用 PM2 或类似工具
pm2 start api/index.ts --name union-staff-backend

# 或使用 Node.js
node --loader ts-node/esm api/index.ts
```

#### 4. 反向代理配置

使用 Nginx 配置示例：

```nginx
server {
    listen 80;
    server_name your-domain.com;
    
    # 前端静态文件
    location / {
        root /path/to/union-staff-archive-system/dist;
        try_files $uri $uri/ /index.html;
    }
    
    # API 反向代理
    location /api/ {
        proxy_pass http://localhost:3001;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }
    
    # 上传文件访问
    location /uploads/ {
        alias /path/to/union-staff-archive-system/api/uploads/;
    }
}
```

#### 5. 数据库备份

```bash
# 定时备份脚本（crontab）
0 2 * * * pg_dump -U postgres union_staff | gzip > /backups/union_staff_$(date +\%Y\%m\%d).sql.gz
```

### 开发模式

```bash
# 同时启动前后端热更新
pnpm run dev

# 仅启动前端
pnpm run client:dev

# 仅启动后端
pnpm run server:dev
```

### 常用命令

| 命令 | 说明 |
|------|------|
| `pnpm run dev` | 启动开发服务器（前后端） |
| `pnpm run build` | 生产构建 |
| `pnpm run check` | TypeScript 类型检查 |
| `pnpm run lint` | ESLint 代码检查 |
| `pnpm run preview` | 预览生产构建 |

---

## 常见问题排查

### 数据库相关

#### Q1: 连接数据库时提示 "connection refused"

**原因**：PostgreSQL 服务未启动或配置错误。

**解决**：
```bash
# Windows 下检查服务
Get-Service -Name postgresql*

# 启动服务
Start-Service postgresql-x64-14
```

#### Q2: 初始化脚本报 "字符编码不匹配"

**原因**：Windows 环境默认使用 GBK 编码，无法正确处理 SQL 中的中文。

**解决**：设置 UTF-8 编码环境变量
```powershell
Set-Item -Path env:PGCLIENTENCODING -Value UTF8
```

#### Q3: 提示 "关系不存在"

**原因**：未执行初始化脚本或数据库版本不匹配。

**解决**：重新执行初始化脚本
```powershell
Set-Item -Path env:PGCLIENTENCODING -Value UTF8
psql -U postgres -d union_staff -f api/database/init.sql
```

### 应用相关

#### Q4: 登录提示 "账号或密码错误"

检查以下几点：
1. 使用的账号邮箱是否正确（默认：`admin@union.com`）
2. 密码是否正确（默认：`password`）
3. 数据库中用户表是否已初始化
4. 是否为特殊角色账户（管理员、审核人）

#### Q5: 提交申请时提示 "您已经是工会会员"

**原因**：用户已存在 `union_members` 表中的记录。

**解决**：检查用户状态，如需重新申请需管理员协助处理。

#### Q6: 困难帮扶提交提示 "同一病种仅能申请一次"

**原因**：该用户已提交过相同病种的申请（任何状态）。

**解决**：
- 更换其他病种
- 或联系管理员处理历史申请

#### Q7: 爱心帮扶提交提示 "请先加入爱心互助会"

**原因**：用户未获取 `mutual_aid_member` 身份。

**解决**：先提交爱心互助会申请并通过审批。

#### Q8: 批量导入失败 "缺少必要字段"

**原因**：Excel 文件表头缺少必填列。

**解决**：确保 Excel 包含"姓名"、"员工编码/身份证号"、"所属单位"、"联系电话"等列。

### 网络相关

#### Q9: 前端访问 API 时出现 CORS 错误

**原因**：开发模式下已配置 Vite 代理。如果使用自定义端口需检查配置。

**解决**：检查 `vite.config.ts` 中的代理配置：
```typescript
server: {
  proxy: {
    '/api': 'http://localhost:3001'
  }
}
```

#### Q10: 上传文件大小限制

后端 Multer 中间件默认限制 10MB。如需调整，修改 `api/routes/difficulty.ts` 中的配置：
```typescript
const upload = multer({ 
  storage,
  limits: { fileSize: 20 * 1024 * 1024 }  // 20MB
})
```

---

## 附录

### 默认数据清单

**默认用户**：

| 角色 | 邮箱 | 密码 | 姓名 |
|------|------|------|------|
| 管理员 | admin@union.com | password | 系统管理员 |
| 基层审核人 | grass_root@union.com | password | 基层审核人 |
| 委员会审核人 | committee@union.com | password | 委员会审核人 |
| 普通职工 | employee@union.com | password | 普通职工 |

**默认病种**：

| 病种名称 | 类别 |
|----------|------|
| 恶性肿瘤 | 重疾 |
| 严重心血管疾病 | 重疾 |
| 慢性肾功能衰竭 | 重疾 |
| 严重肝病 | 重疾 |
| 糖尿病 | 慢病 |
| 高血压 | 慢病 |
| 心脏病 | 慢病 |
| 其他疾病 | 其他 |

**默认模块**：

| 代码 | 名称 | 排序 |
|------|------|------|
| tasks | 任务管理 | 1 |
| membership_audit | 入会审核 | 2 |
| fee_audit | 会费审核 | 3 |
| mutual_aid_audit | 互助会审核 | 4 |
| mutual_aid_difficulty_audit | 爱心帮扶审批 | 5 |
| user_management | 用户管理 | 6 |
| system_logs | 系统日志 | 7 |
| statistics | 查询统计 | 8 |

### 审核状态流转图

```
                    ┌──────────────┐
                    │   提交申请   │
                    └──────┬───────┘
                           │
                    ┌──────▼───────┐
                    │  pending     │  待基层审核
                    └──────┬───────┘
                           │
              ┌────────────┴────────────┐
              ▼                         ▼
       ┌──────────────┐          ┌──────────────┐
       │ 基层审核通过  │          │ 基层审核拒绝  │
       └──────┬───────┘          └──────┬───────┘
              │                         │
              ▼                         ▼
       ┌──────────────┐          ┌──────────────┐
       │grass_root    │          │  rejected    │  流程终止
       └──────┬───────┘          └──────────────┘
              │
              │
              ▼
       ┌──────────────┐
       │  union_       │  待委员会审核
       │  committee    │
       └──────┬───────┘
              │
    ┌─────────┴─────────┐
    ▼                   ▼
 ┌───────────┐     ┌───────────┐
 │委员会审核 │     │委员会审核 │
 │   通过    │     │   拒绝    │
 └─────┬─────┘     └─────┬─────┘
       │                 │
       ▼                 ▼
┌──────────────┐  ┌──────────────┐
│  approved    │  │  rejected    │
│  已通过      │  │  已拒绝      │
└──────────────┘  └──────────────┘
```

---

## 版本历史

| 版本 | 日期 | 说明 |
|------|------|------|
| 1.0.0 | 2024-12 | 初始版本，支持工会会员管理、会费授权、困难帮扶 |
| 1.1.0 | 2025-01 | 新增爱心互助会、爱心帮扶功能 |
| 1.2.0 | 2025-07 | 完善同步申请、一键导入、批量导入等功能 |

---

## 技术支持

如需技术支持，请联系系统管理员或查阅项目源码及文档。
